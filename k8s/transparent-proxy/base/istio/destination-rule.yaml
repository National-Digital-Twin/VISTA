apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: realtime-trains-dr
spec:
  host: api.rtt.io
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 50
      http:
        http1MaxPendingRequests: 50
        maxRequestsPerConnection: 10
