apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    io.kompose.service: paralog-transparent-proxy
  name: paralog-transparent-proxy
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 80
  type: NodePort
  selector:
    io.kompose.service: paralog-transparent-proxy
