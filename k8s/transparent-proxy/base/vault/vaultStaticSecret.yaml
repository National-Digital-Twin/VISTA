apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: vista-transparent-proxy-static-secret
spec:
  refreshAfter: 30s
  # path of the secret
  path: applications/vista/transparent-proxy
  type: kv-v2
  mount: k8-cluster
  destination:
    # destination k8s secret
    name: vista-transparent-proxy-secrets
    create: true
  vaultAuthRef: vista-transparent-proxy-vault-auth
