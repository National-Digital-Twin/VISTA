apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: paralog-transparent-proxy-static-secret
spec:
  refreshAfter: 30s
  # path of the secret
  path: applications/paralog/transparent-proxy
  type: kv-v2
  mount: k8-cluster
  destination:
    # destination k8s secret
    name: paralog-transparent-proxy-secrets
    create: true
  vaultAuthRef: paralog-transparent-proxy-vault-auth
