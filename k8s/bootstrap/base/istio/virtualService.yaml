apiVersion: networking.istio.io/v1alpha3
kind: VirtualService

metadata:
  name: paralog-virtual-service
spec:
  hosts:
    - placeholder
  gateways:
    - placeholder
  http:
    - match:
        - uri:
            prefix: /ndtp-python/
      rewrite:
        uri: "/"
      route:
        - destination:
            host: paralog-python-api
            port:
              number: 80
    - match:
        - uri:
            prefix: /paralog/
      rewrite:
        uri: "/"
      route:
        - destination:
            host: smart-cache-paralog-api
            port:
              number: 80
    - match:
        - uri:
            prefix: /transparent-proxy/
      rewrite:
        uri: "/"
      route:
        - destination:
            host: paralog-transparent-proxy
            port:
              number: 80
    - match:
        - uri:
            prefix: /
      rewrite:
        uriRegexRewrite:
          match: ^/(/|$)(.*)
          rewrite: /\2
      route:
        - destination:
            host: paralog-frontend
            port:
              number: 80
