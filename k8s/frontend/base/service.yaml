apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    io.kompose.service: paralog-frontend
  name: paralog-frontend
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 80
  type: NodePort
  selector:
    io.kompose.service: paralog-frontend
