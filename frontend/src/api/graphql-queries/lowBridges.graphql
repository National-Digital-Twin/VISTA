query getLowBridges {
  lowBridges(
    lowBridgeInput: { latMin: 50.0, latMax: 50.9, lonMin: -1.3, lonMax: 0.5 }
  ) {
    name
    localId
    direction
    dimensionIn
    latitude
    longitude
  }
}
